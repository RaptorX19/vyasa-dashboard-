# Copy to .env (or export in your shell) and fill in.
# Enables the AI "Discover competitors" web-search feature.
OPENAI_API_KEY=sk-...

# Optional: override the OpenAI model (default gpt-4o).
# OPENAI_MODEL=gpt-4o

# Optional: port the server listens on (default 5050)
PORT=5050
